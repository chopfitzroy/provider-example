{
  "name": "module-example",
  "version": "1.0.0",
  "description": "Demo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:supersonic": "microbundle build --raw --cwd supersonic"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "microbundle": "^0.11.0"
  }
}
